const name = "Susan"; // Change "Joe" to "Susan"
const height = 35; // Set a value less than 40
const message = `${name} is ${height} inches tall`; // Ensure message reflects changes

module.exports = { name, height, message };
